export interface StackType {
  mapCode: string;
  mapName: string;
}
export interface PointType {
  name: string;
  value: number[];
}

export interface ScaleLimitType {
  min: number;
  max: number;
}
